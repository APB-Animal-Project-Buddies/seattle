table:
  name: projects
  schema: public
array_relationships:
  - name: account_snapshots
    using:
      foreign_key_constraint_on:
        column: project_handle
        table:
          name: account_snapshots
          schema: public
  - name: project_snapshots
    using:
      foreign_key_constraint_on:
        column: project_handle
        table:
          name: project_snapshots
          schema: public
